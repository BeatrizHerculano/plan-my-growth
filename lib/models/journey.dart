class Journey {
  String title = "";
  String description = "";
  String dateText = "";
  DateTime dateTime;
  bool done = false;

  Journey(this.title, this.description, this.dateTime, this.dateText);
}
